// packages/ui-kit — shared design-system primitives (web-focused, used by
// apps/ui). Design tokens live in ./theme.css (Tailwind v4, extends
// apps/ui's tokens — see that file for the consolidation plan).
//
// Empty scaffold (Phase 0). No components yet.
export {};
