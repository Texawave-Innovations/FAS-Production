{
  "name": "@fas-erp/database",
  "version": "0.0.0",
  "private": true,
  "description": "Prisma schema, migrations, and seed scripts — the single source of truth for the data model. Owned/consumed by apps/api via the generated Prisma client. Empty schema — Phase 0.",
  "type": "module",
  "main": "./generated/client/index.js",
  "types": "./generated/client/index.d.ts",
  "exports": {
    ".": {
      "types": "./generated/client/index.d.ts",
      "default": "./generated/client/index.js"
    }
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "build": "prisma generate",
    "typecheck": "echo 'no TS sources yet — schema.prisma is validated by `prisma generate`'"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "prisma": "^6.19.3",
    "typescript": "^5.7.0"
  }
}
