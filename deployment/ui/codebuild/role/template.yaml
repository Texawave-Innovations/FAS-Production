# Placeholder CloudFormation template — CI plumbing for apps/ui.
# Defines the CodeBuild role (this rarely changes once set up).
# Distinct from apps/ui/deployment/fas-erp/{dev,uat,prod}/template.yaml,
# which is the environment-specific stack that actually gets deployed
# (see FAS_ERP_Architecture_Guide.md §10).
