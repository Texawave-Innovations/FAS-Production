# Placeholder Dockerfile for apps/api (NestJS).
# TODO(Phase 8 — Hardening): multi-stage build (pnpm install --frozen-lockfile,
# nest build, prune devDependencies, copy dist + node_modules into a slim
# runtime image, non-root user, HEALTHCHECK). Left empty intentionally —
# Phase 0 is structure/tooling only, no build logic yet.
