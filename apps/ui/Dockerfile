# Placeholder Dockerfile for apps/ui (Next.js).
# TODO(Phase 8 — Hardening): multi-stage build (pnpm install --frozen-lockfile,
# next build with standalone output, copy .next/standalone + public + static
# into a slim runtime image, non-root user, HEALTHCHECK). Left empty
# intentionally — Phase 0 is structure/tooling only, no build logic yet.
